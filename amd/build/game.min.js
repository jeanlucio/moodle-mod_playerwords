/**
 * AMD module for mod_playerwords game interactions.
 *
 * Handles the forfeit confirmation dialog and the post-round cooldown countdown.
 *
 * @module     mod_playerwords/game
 * @copyright  2026 Jean Lúcio
 * @license    https://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("mod_playerwords/game",["core/modal_save_cancel","core/modal_events","core/str"],(function(ModalSaveCancel,ModalEvents,Str){var tick=function(el,until){var remaining=until-Math.floor(Date.now()/1e3);if(remaining<=0)el.textContent=el.dataset.ready;else{var h=Math.floor(remaining/3600),m=Math.floor(remaining%3600/60),s=remaining%60,parts=[];h>0&&parts.push(h+"h"),parts.push(String(m).padStart(2,"0")+"m"),parts.push(String(s).padStart(2,"0")+"s"),el.textContent=parts.join(" "),window.setTimeout((function(){tick(el,until)}),1e3)}};return{init:function(cooldownUntil){var form,until,el;(form=document.getElementById("playerwords-forfeit-form"))&&form.addEventListener("submit",(function(e){e.preventDefault(),Promise.all([ModalSaveCancel.create({title:form.dataset.title,body:form.dataset.confirm,show:!0,removeOnClose:!0}),Str.get_string("yes","core")]).then((function(results){var modal=results[0],yesStr=results[1];modal.setSaveButtonText(yesStr),modal.getRoot().on(ModalEvents.save,(function(){form.submit()}))})).catch(window.console.error)})),cooldownUntil>0&&(until=cooldownUntil,(el=document.getElementById("playerwords-cooldown-countdown"))&&tick(el,until))}}}));

//# sourceMappingURL=game.min.js.map