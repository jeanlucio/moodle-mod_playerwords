/**
 * AMD module for mod_playerwords attempts-report page interactions.
 *
 * Provides select-all, bulk delete and single-row delete via a shared form — the same
 * pattern mod_playerwords/managewords already uses for its own bulk actions, trimmed
 * down to delete-only (no approve concept here).
 *
 * @module     mod_playerwords/attemptsreport
 * @copyright  2026 Jean Lúcio
 * @license    https://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("mod_playerwords/attemptsreport",["core/modal_save_cancel","core/modal_events","core/str"],(function(ModalSaveCancel,ModalEvents,Str){let bulkForm=null,bulkActionField=null,selectAllCheckbox=null,bulkDeleteBtn=null;const updateBulkButton=()=>{const totalCount=document.querySelectorAll(".playerwords-attempts-bulk-check:checked").length;bulkDeleteBtn&&(bulkDeleteBtn.disabled=0===totalCount,bulkDeleteBtn.textContent="".concat(bulkDeleteBtn.dataset.labelbase," (").concat(totalCount,")"))},showModal=async(title,body,onConfirm)=>{try{const[modal,yesStr]=await Promise.all([ModalSaveCancel.create({title:title,body:body,removeOnClose:!0}),Str.get_string("yes","core")]);modal.setSaveButtonText(yesStr),modal.getRoot().on(ModalEvents.save,(()=>onConfirm())),modal.show()}catch(error){window.console.error(error)}};return{init:function(){selectAllCheckbox=document.getElementById("playerwords-attempts-select-all"),selectAllCheckbox&&(selectAllCheckbox.addEventListener("change",(()=>{document.querySelectorAll(".playerwords-attempts-bulk-check").forEach((cb=>{cb.checked=selectAllCheckbox.checked})),updateBulkButton()})),document.querySelectorAll(".playerwords-attempts-bulk-check").forEach((cb=>{cb.addEventListener("change",(()=>{const total=document.querySelectorAll(".playerwords-attempts-bulk-check").length,checked=document.querySelectorAll(".playerwords-attempts-bulk-check:checked").length;selectAllCheckbox.checked=checked===total,selectAllCheckbox.indeterminate=checked>0&&checked<total,updateBulkButton()}))}))),bulkForm=document.getElementById("playerwords-attempts-form"),bulkActionField=document.getElementById("playerwords-bulk-action"),bulkDeleteBtn=document.getElementById("playerwords-attempts-bulk-delete-btn"),bulkForm&&bulkDeleteBtn&&bulkDeleteBtn.addEventListener("click",(()=>{showModal(bulkDeleteBtn.dataset.title,bulkDeleteBtn.dataset.confirm,(()=>{bulkActionField&&(bulkActionField.value="delete"),bulkForm.submit()}))})),document.querySelectorAll(".playerwords-attempts-single-delete-btn").forEach((btn=>{btn.addEventListener("click",(()=>{const attemptid=btn.dataset.attemptid;document.querySelectorAll(".playerwords-attempts-bulk-check").forEach((cb=>{cb.checked=cb.value===attemptid})),selectAllCheckbox&&(selectAllCheckbox.checked=!1,selectAllCheckbox.indeterminate=!1),showModal(btn.dataset.title,btn.dataset.confirm,(()=>{bulkActionField&&(bulkActionField.value="delete"),bulkForm.submit()}))}))}))}}}));

//# sourceMappingURL=attemptsreport.min.js.map